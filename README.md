# ASYNC-TCP-SERVER

The TCP server is written by C can handle multiple concurrency clients.

## Usage

Build it

```sh
> gcc async_tcp_server.c -o async_tcp_server
```

Run

```sh
> ./async_tcp_server 0.0.0.0 9999
Listen TCP server at 0.0.0.0 9999
```
